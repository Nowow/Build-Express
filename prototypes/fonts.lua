local constants = require("constants")

local bluid_blueprint_hotkey_font ={
    type = "font",
    name = constants.catch_blueprint_order_hotkey_font,
    from = "default-bold",
    spacing = '0.5',
    size = 30
}

data:extend({bluid_blueprint_hotkey_font})